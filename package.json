{
  "name": "grpc-library-project",
  "version": "1.0.0",
  "description": "Root package for managing the polyglot gRPC library system",
  "scripts": {
    "start": "docker compose up",
    "start:build": "docker compose up --build",
    "stop": "docker compose down",
    "clean": "docker compose down -v"
  }
}
